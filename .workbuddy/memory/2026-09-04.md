# 2026-09-04

## GUI 中文化 + 界面重设计（commit bf5a2f0）
- 新增 src/i18n.rs：Lang 枚举（Zh/En）+ Tr 静态文案表，标题栏「English/中文」一键切换
- Config 新增 lang 字段（serde default "zh"，旧配置文件兼容），切换语言立即持久化
- app.rs 界面重写：
  - 标题栏（品牌 + tagline + 语言切换按钮）
  - 左侧改为分组卡片（基本设置 / 屏幕与客户端 / 运行状态），全宽保存按钮，3 秒自动消失的 toast 提示
  - 中央改为深色画布：屏幕卡片带投影、hover 高亮、抓手光标、图例（主机/本机/客户端）、底部提示
  - 默认窗口 1120×720，最小 860×560
- 启动错误 banner 改为红色卡片样式（文案随语言切换）
- 排坑：
  - MutexGuard 存活期间不能调 &mut self 方法 → 改字段直接赋值（disjoint borrow）
  - egui Response::on_hover_cursor 是 builder 消费自身 → 用 if/else 重绑定
  - macOS debug 构建启动即 panic（icrate NSEnumerator 'q' vs 'Q' 类型编码校验）→ objc2 debug 校验所致，release 构建正常，与 UI 代码无关
- 本地 cargo build / release 构建均通过；已运行 release 版验证（hub 正常监听、字体加载成功）
- 注意：预览前用 osascript 退出了旧版 /Applications/MouseShare.app（占端口 49152）；/Applications 里的 App 仍是旧版，需等 CI 出新 dmg 或手动替换

## 自动化发版任务（首次执行）
- 检查仓库状态：与 origin/main 同步（0 0），唯一改动是 .workbuddy/memory/2026-09-03.md
- 按规则走「仅记忆文件改动」分支：提交 13767a9 `docs: update memory` 并 push，未打 tag/发版
- 核查 v0.3.3（commit 702f6a8）发版健康度：
  - 三平台 CI（ubuntu / windows / macos）全部 completed+success
  - Release v0.3.3 资产齐全：mouseshare-linux.deb、mouseshare-linux.tar.gz、mouseshare-mac.dmg、MouseShare-Setup.exe、mouseshare-windows.zip
